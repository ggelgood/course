# 工作日誌

> 每次工作結束前，Claude 會在最上面加一筆。
> 換電腦或隔一段時間回來時，看最上面幾筆就知道進度到哪、上次卡在哪。
>
> **格式**：日期 · 在哪台電腦 · 做了什麼 · 下次要做什麼

---

## 2026-08-17（第三次）· 主力電腦（D:\Claude）

**做了什麼**
- 老師拍好素材並用壓縮工具壓過（7～63 KB，都遠低於目標），**8 張全部接進打字第 1 課**
  - 建立 `網站/media/`，圖片都放這裡
  - 開場故事加了三張：**英文打字機 → 外國漫畫的誤解 → 真正的中文打字機**
    這個對比很有力：漫畫以為「幾萬個字就要幾萬個鍵」，
    真機器其實**一個鍵都沒有**，是用「找」的。中間插一題預測讓學生先猜。
  - 第 6 節放數字鍵盤特寫，用照片裡**印在鍵上的第二個名字**（7 Home、8 ↑⋯⋯）
    當證據，讓學生自己發現「這些鍵有兩種身分」，再接到第 7 節的 Num Lock
  - **三顆燈刻意不放照片**（老師指定）——燈太小，照片看不出亮暗差別，
    改成「抬頭看真的那台」＋ 講義裡的模擬器
- 第 3 節（打字區）新增 **符號輸入測試 `symtest`**：
  「請打出 ？」學生真的在框裡打一次，要自己找鍵、想到按 Shift
- `lesson.css` 新增 **`.shot--done` ＋ `figcaption`**：素材補上去之後，
  佔位框的「要拍什麼／怎麼拍」那些對學生是雜訊，換成正常圖說。
  **17 課 Scratch 之後補圖時也用這個**
- 版本號全部 +1：`lesson.css/js?v=3`、`typing.css/js?v=2`、`home.css?v=3`（19 頁）

**要注意的事（這次新增）**
- `symtest` 有兩個一定要處理的細節，改的時候不要拿掉：
  1. **全形／半形**——中文輸入法打出來是 `？`，英文是 `?`，
     程式會把全形（U+FF01–FF5E）減 0xFEE0 換成半形再比對，**兩種都算對**
  2. **組字中不判定**——打注音時輸入框會先出現ㄅㄆㄇ，
     要等 `compositionend` 才比對，不然學生打到一半一直看到 ✗

---

## 2026-08-17（第二次）· 主力電腦（D:\Claude）

**做了什麼**
- 開了新系列「**中文打字**」，寫好 `docs/中文打字-課程地圖.md`
- 定案為 **4 節**（老師原本想 3 篇，第一篇估時 25 分會爆掉，拆成兩節）
  1. 鍵盤大導覽（五區 ＋ 三顆燈）2. 注音怎麼變成中文字
  3. 手不要離開鍵盤（改錯字＋快捷鍵＋截圖）4. 輸入法大觀園
- **第 1 課做完了**：`網站/typing/01-鍵盤大導覽.html`
- 新增 `網站/assets/typing.css` ＋ `typing.js`（打字系列專用，Scratch 課不載入）
  做了三個新互動：**會動的鍵盤全圖**（點分區會亮）、**三顆燈實驗**
  （關掉 Num Lock 數字真的打不出來）、**游標實驗室**（Backspace／Delete／Insert 陷阱）
- 首頁加上「中文打字」課程區塊，資訊素養那區的「打字練習」拿掉避免重複
- `網站/README.md` 補上新元件用法；**修正版本號指令**（原本只掃 `lessons\`，
  漏掉新的 `typing\` 和 `index.html`）
- `素材清單.md` 加上打字第 1 課的 6 張素材（多半是實體鍵盤照片）

- 做了 **`網站/tools/圖片壓縮.html`**（首頁「給老師」區塊有連結）
  - 拖放圖片 → 自動縮到 1600px → 壓縮 → 顯示省了幾 %，用顏色標有沒有達標
  - **單一檔案、零外部相依**，可以丟到隨身碟在任何電腦離線使用
  - 實測：2560×1440 的 3.7 MB 截圖 → WebP 100 KB（省 97%）、JPEG 172 KB（省 95%）
  - **遇到 GIF 會擋下來不處理**（見下方注意事項）
  - ⚠️ **這是一個通用工具，刻意不寫任何講義相關的東西**（怎麼接進課頁、
    素材檔名規則⋯⋯那些寫在 `素材清單.md`）。之後要加功能請維持這個界線。
- 做了 **`網站/tools/圖片裁剪.html`**（同樣是通用工具、單一檔案、零外部相依）
  - 拖曳裁剪框、八個把手改大小、比例鎖定（1:1／16:9／4:3／3:4／9:16／原圖比例）
  - 方向鍵可以一格一格微調（配 Shift 一次 10 格），轉向 90°（手機照片常常是躺著的）
  - 實測：座標換算精準（移動 200 原圖像素＝ 67.7 顯示像素）、
    拖出界會夾在圖片邊緣、鎖比例時亂拉比例都維持在 1.778
  - 兩個工具互相連結：**先裁再壓**
- `素材清單.md` 補上建議大小的表格、壓縮工具用法、`loading="lazy"` 的提醒

**下次要做什麼**
- 做打字第 2 課「注音怎麼變成中文字」——核心是**注音鍵位的直行規律**
  （ㄅㄆㄇㄈ＝1QAZ 一直行），地圖裡已經寫好完整的對照表
- 第 2 課要新做 `bpmkeys`（滑過會亮整條直行）和 `typerace`（打字練習）兩個元件

**要注意的事**
- ⚠️ **GIF 不能用瀏覽器壓縮**。把圖畫到 canvas 再輸出，動畫只會剩第一格，
  而我們的素材有 39 個是 GIF。所以壓縮工具遇到 GIF 會**擋下來並說明**，
  不會產生下載連結（避免老師拿到被毀掉的檔案）。
  GIF 要在 **ScreenToGif 錄的時候**就調小，事後救不回來。
- 圖片建議大小：截圖 200–400 KB、照片 150–300 KB、GIF 0.8–2 MB，
  **整頁加起來不超過 3 MB**（限制是學校網路速度，不是容量）
- 裁剪工具的 `.stage` 上有 `overflow:hidden`，**不要拿掉** ——
  裁剪框是用一個 9999px 的 `box-shadow` 把框外壓暗的，
  沒有那個裁切的話陰影會蓋滿整個頁面。把手也刻意畫在框「裡面」，
  不然框拖到圖片邊緣時會被切掉一半。
- ⚠️ **`typerace` 有個大坑**：學生打注音時輸入框會有「組字中」狀態，
  直接聽 `input` 事件會把組字中的注音判成打錯。**一定要用
  `compositionstart` / `compositionend`**，只在組字結束後才比對。
- ⚠️ 講義裡有兩處**史實與字碼要老師覆核再上線**：第 1 課故事的年份
  （林語堂 1947、朱邦復 1976）、第 4 課的倉頡字碼。**無蝦米的字碼沒有可靠來源，
  講義不會寫死按鍵**，只講「用字形像的英文字母」這個原則。
- 第 3 課的截圖鍵 `Win+Shift+S` 是作業系統層級的，**網頁收不到那個按鍵**，
  所以不能做成按鍵偵測練習，要改用 GIF 示範
- 新增了 `.claude/launch.json`（本機預覽用，跟講義內容無關）

---

## 2026-08-17 · 主力電腦（D:\Claude）

**做了什麼**
- 完成全部 17 課講義，互動元件全部實測通過
- 完成課程總覽首頁 `網站/index.html`，含三個預留課程區塊
  （Scratch 進階 / Arduino / 資訊素養）
- 每一課加上「← 課程總覽」返回連結
- 建立 GitHub repo `ggelgood/course`，設定 GitHub Actions 自動發布
- 網站上線：https://ggelgood.github.io/course/
- 產出 `素材清單.md`（50 個待拍素材，含檔名與拍攝說明）
- 加上 `START-HERE.md` 接手指南與這份工作日誌

**下次要做什麼**（老師還沒決定要先做哪一個）
- 拍素材，然後接進講義（50 個，優先拍標 ★ 的 15 個）
- 或製作範例 `.sb3` 檔案放進 `網站/files/`
- 或開始新的課程單元（Scratch 進階／Arduino／資訊素養）

**要注意的事**
- 素材資料夾 `網站/media/` 和 `網站/files/` 目前是空的，
  git 不追蹤空資料夾，所以在 GitHub 上看不到它們，這是正常的
