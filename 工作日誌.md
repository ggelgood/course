# 工作日誌

> 每次工作結束前，Claude 會在最上面加一筆。
> 換電腦或隔一段時間回來時，看最上面幾筆就知道進度到哪、上次卡在哪。
>
> **格式**：日期 · 在哪台電腦 · 做了什麼 · 下次要做什麼

---

## 2026-09-01（第十次）· 主力電腦（D:\Claude）

> ⚠️ 第九次（2026-08-18）到這一次之間有幾次工作沒有寫進日誌
> （資訊素養第 1～3 課就是那段時間完成的），這一筆之後記得補回習慣。

**做了什麼**
- **資訊素養新增第 4 課「驗證碼：電腦在問『你是人嗎？』」**：
  `網站/literacy/04-驗證碼你是人嗎.html`，七個段落。同時在首頁開了
  **單元三：網路安全**（目前 1 堂），資訊素養從 3 堂變 4 堂。
- 這一課的重概念是**「驗證碼是在確認什麼」**，不是「怎麼打驗證碼」。
  中文裡「驗證碼」同時指兩件事——網站上擋機器人的那串扭曲字，
  和手機簡訊收到的六個數字。**只教後者的話，學生會記成
  「驗證碼＝危險，不要打」這條錯規則**，所以兩種放同一節對照，
  用一句話收束：**驗證碼只能「打進去」，不能「傳出去」**。
- `literacy.css` 新增元件 9～13、`literacy.js` 新增元件 4～6：
  - `botrace`：人 vs 機器人比手速，10 秒後算出「它是你的幾倍」。
    這一段是整節地基——差距要用身體感覺，不能用講的。
  - `cap`：真的會產生扭曲驗證碼的練習台。字元集**刻意拿掉 0/O、1/l/I**
    （易混字另開一格 `.mixup` 講，不拿來坑學生），打錯的回饋分三種，
    照 `.predict` 的原則寫成「你是哪一種錯」。
  - `picap`：九宮格選圖，**每題目標數量 2～4 個浮動**，
    固定的話學生第二題就開始用「選三個」蒙過去。
  - `twocap`／`oneline`／`scam`／`sms`：第 6 段那組安全對照圖與詐騙話術卡。
- 詐騙那一段刻意**不教「怎麼分辨真假客服」**——中年級分不出來，
  教了反而讓他以為自己有能力判斷。給的是一條不用思考的規則：
  **只要有人跟你要驗證碼，答案都是「不給」**，然後告訴大人、165。
- **老師第一次修改：收尾整段重寫**。原本的動手做是五件事，後三件
  「跟同學說出哪一種可以打」「兩人一組演一次拒絕」「回家問爸媽」——
  老師的評語是「太幼稚且沒有意義」，說得對：**這三件驗不出學生懂不懂，
  也沒有任何思考量**。改成沿用單元二的收尾做法（第 2、3 節是畫一張流程圖），
  換成**反過來自己出一題驗證碼**：先給三個合格條件
  （人五秒內會、程式做不到、答案只有一個），學生出題後逐條自己檢查，
  再給同學做一次來驗第 3 條——**同學在這裡是驗證工具，不是聽眾**。
  配一個揭曉放四題「不合格」的題目並指出各壞在第幾條。
  原本掛在求救框的「先背這一句拒絕的話」是配合演戲那一步的，
  演戲拿掉後把它併進第 6 段的 🚨 框（那裡才是它該待的地方）。
- `literacy.css` 再加元件 14 `.rulelist`（編號條件清單）。
  刻意不用 `.steps`——`.steps` hover 會預覽打勾，學生看到就會去點，
  但這三條是拿去對照的判準，不是做完打勾的步驟。
- 共用檔改過，版號一起跳：`literacy.css?v=9→11`、`literacy.js?v=6→7`
  （四課全部同步）。
- 文件同步：`docs/資訊素養-課程地圖.md` 補上單元三整段設計理由；
  `素材清單.md` 標明第 2、3、4 課不需要任何素材（全部是 CSS／JS 畫的）。

**驗過了什麼**
- 本機起 `http.server` 實跑：六題 `.predict` 全部答對會亮綠、
  `cap` 的三種錯誤回饋分支都對、`picap` 選錯標紅／漏選標綠、
  `botrace` 10 秒跑完會結算，桌機與 375px 都沒有橫向溢出。
- 順手修掉一個 bug：`botrace` 結算時「時間到！」被 `paint()` 蓋回
  「還有 0.0 秒」，把 `paint(secs)` 挪到設字之前。

**下次要做什麼**
- 單元三還要補「密碼怎麼設」和「個資保護」。
  「密碼」那一節可以直接接這一節的比手速（機器人一秒試幾千組密碼）。
- 補回 8/18～8/31 那幾次沒寫的日誌（資訊素養第 1～3 課）。

---

## 2026-08-18（第九次）· 主力電腦（D:\Claude）

**做了什麼**
- **打字第 4 課「輸入法大觀園」全部完成，中文打字系列到此四課全數完工**：
  `網站/typing/04-輸入法大觀園.html`。先給老師大綱過目，老師把原本規劃的
  「換你當發明家」收尾拿掉（「這不是電腦課的重點，抱歉」），改成
  **總複習 ＋ 選擇你的輸入法**（選一種並說出具體理由，沒有標準答案，紙上反思，
  不做成網頁互動題）。
- 新增兩個元件到 `typing.css`／`typing.js`：`imecbox`（明／林／森三個字，
  四種輸入法並排卡，點字切換，純展示不判對錯）、`radbox`（倉頡字根拼字玩具，
  點日月金木水火土疊字，純玩具不出考題）。無蝦米沒有可靠字碼來源，
  只講「用字形像的字母」原則，不寫死具體按鍵。
- **老師二次修改**：把「無蝦米怎麼拆」那個補充框整段拿掉（不用提）；
  `radbox` 加上老師親自給的倉頡對照表（七字根 7×7＝49 種兩碼組合），
  現在可以點兩個字根、按「組合」，真的拼出「明」「林」這種字，答對了框會變綠、
  按「清空」繼續玩下一組。補充框改放老師給的「鬱」倉頡碼彩蛋
  （木木竹月山木戈，七個字根疊成一個字，示範字根不只七個）。
- **老師三次修改**：字根玩具改成最多只能點兩個（對照表本來就只收兩碼組合），
  第三下直接不加，不寫任何說明文字，安靜生效就好。
  最後的反思活動②「為什麼」改成「問問你身邊的大人，他們平常打中文字用的是哪一種方法」，
  從自己講理由變成回家做一個小訪問，`sos` 提示框文字也一併順過。
- **老師四次修改**：無蝦米整個從這課拿掉（「本篇無蝦米都不用提到」）——
  兩派對照卡、`imecbox` 四字並排卡都改成只留注音／拼音／倉頡，「四種輸入法」
  相關的標題文案也跟著改成「三種」。`docs/中文打字-課程地圖.md` 裡原本第 4 課大綱
  還提到無蝦米，現在跟實際講義不一致了，之後改大綱記得同步。
- **老師五次修改**：把「全部只分兩派（用唸的／用拆的）」的二層分類法拿掉，
  改成三個平行的派別——**第一派注音、第二派拼音、第三派拆解**，對中年級來說
  更具體好懂。以「明」字舉例時，先秀出大大的「明」字（沿用 `imec__ch` 樣式），
  再三張卡並排三派打法。`typing.css` 新增第三種顏色（`--c`，magenta，
  `--b-sound-d`）給 `.vs__c`／`.imec__c` 用，`.vs__c--big` 給範例文字放大置中。
  section 3 的並排卡標籤也一併從「注音・用唸的」改成「第一派・注音」，
  跟第 2 節用同一套說法。
- `typing.css?v=9`、`typing.js?v=10`，01～04 四課版本號已同步。
- 首頁 `index.html` 把中文打字第 4 節從「準備中」的 soonbox 換成正式課卡。
- `課程地圖.md`（打字系列那份）、`素材清單.md` 都補上第 4 課已完成的記錄——
  第 4 課跟第 2 課一樣**沒有素材需求**（純概念課，不需要截圖／GIF）。
- 在本機用 `http.server` 起服務，實際點過 `imecbox` 分頁切換、`radbox` 點字根／清空，
  確認互動都正常，畫面也對過。

**下次要做什麼**
- 中文打字系列四課全部完工，之後可以拍素材：第 3 課還缺 1 個 ★
  （`03-1-截圖流程.gif`），Scratch 17 課的 50 個素材全部待拍。
- 或是開始 Scratch 進階／Arduino／資訊素養這三個預留區塊的內容。

**要注意的事**
- 已經 commit（訊息「01」）+ push 過了，本機和 origin/main 完全同步，乾淨。
- 課程地圖裡「重概念：所有輸入法只分兩派」那段表格是最原始的草稿，
  跟最終改成「三派」的實際講義**不一致**，是歷史紀錄，之後重寫大綱要注意。

---

## 2026-08-18（第八次）· 主力電腦（D:\Claude）

**做了什麼**
- **打字第 2 課「注音怎麼變成中文字」全部完成**：直行規律（新元件 `bpmbox`，滑過亮整條直行）、
  聲調鍵、🌟 語詞自動選字秘訣（從原本第 3 課的鉤子提前搬過來，`wpick` 元件按鍵次數 21 下 vs 14 下）、
  整句打字練習（`trace` 元件，刻意不計時）。第 2 課有一段「大人是怎麼打字的」補充
  （基本鍵/定位鍵），定位成純知識性介紹，不練習不考，跟「不教盲打」政策沒有衝突。
- **打字第 3 課「手不要離開鍵盤」全部完成**：不整句重打（沿用第 1 課 `.caret`）、
  組合鍵要按住、快捷鍵大集合（新元件 `hotkey`：Ctrl+Z/C/V/X/A、單獨的 Shift）、截圖工作流
  （Win 鍵組合網頁收不到，改用 GIF 示範，不做互動元件）。老師指示拿掉了 Insert 陷阱那段
  （太少見）、拿掉了「Ctrl+S 這課不教」那個框（那是備忘錄口吻，不是講義該有的內容）。
- **修過兩個真的會讓學生體驗變差的 bug**：
  ① 注音鍵盤字太小又排不齊，根因是 `.bk` 用 `<button>` 裝文字卻沒寫 `font:inherit`，
  瀏覽器會把按鈕字級重設掉；點擊後「意外」變正常是撞到 `lesson.css` 同名的 `.pick` class，
  不是設計。已經在 `.bk` 補上 `font:inherit` 徹底解決。
  ② **選字最後一步整篇寫成空白鍵，正確應該是 Enter**（空白鍵是聲調／一聲，Enter 才是送出）。
  這個錯誤源頭在 `課程地圖.md`，第 1、2 課和地圖都已訂正，以後寫中文輸入流程送出一律是 Enter。
- `hotkey` 元件的 Shift 單獨按判斷，長按會被瀏覽器重複觸發 keydown 誤判成失敗，已修正邏輯
  並用模擬按鍵事件驗證過。

**下次要做什麼**
- 第 3 課還缺 2 個 GIF（截圖流程、Home 跳到最前面對比 slower 滑鼠點），已列進 `素材清單.md`。
- 第 4 課「輸入法大觀園」還沒開始，要先給大綱、老師點頭再寫內容。

**要注意的事**
- 已經 commit + push 過了，`git status` 跟 origin 同步，乾淨。
- 用 `<button>` 裝文字、字級靠繼承的元件，一定要記得寫 `font:inherit`，
  不然瀏覽器會把按鈕字級重設成系統預設值，這個坑很隱蔽，肉眼從結果很難聯想到原因。

---

## 2026-08-17（第七次）· 主力電腦（D:\Claude）

**做了什麼**
- **第四區補上數學符號介紹**：重點是 **× 要打成 `*`、÷ 要打成 `/`**
  （× 跟字母 x 太像會分不出來；/ 則是「分數線本來就是斜的」，1/2）。
  其他 `+ - .` 和「數字鍵盤的 Enter ＝ 小算盤的等於」用一小段列點帶過。
  結尾回扣課文主軸：**鍵盤上放不下的東西就得用現有的鍵代替** ——
  ×÷ 換成長得像的符號、中文標點用 Ctrl 配出來，是同一個道理。
- 大鍵盤上的 `/ * - + ⏎ .` 六顆改成**可以點的鍵**（點了會說明），
  可點的鍵從 17 顆變 23 顆，三張鍵盤都有（複製機制自動帶過去）。
- **修復 `.kbd__say` 不見的問題**：那是點鍵之後顯示說明的那一行，
  在某次編輯中從原始的 `.kbdbox` 裡消失了，連帶三張鍵盤都失去說明功能
  （點鍵沒反應）。已補回，並確認三張都正常。

---

## 2026-08-17（第六次）· 主力電腦（D:\Claude）

**做了什麼**（第 1 課再修六項）
- **「，」「。」的錯誤提示補上打法**：改成「試試看按住 <b>Ctrl</b> 再按逗號鍵（Ctrl ＋ ,）」，
  答對後也會顯示 `Ctrl ＋ ,` / `Ctrl ＋ .`。原本只說「你是英文輸入法」卻沒告訴學生怎麼打。
- **重寫「中文的，。和英文的, .」那段** —— 老師說得對，那段在講一個學生用不到的冷知識。
  改成 **「特殊符號有兩種打法」**：① `Shift ＋ 鍵` 打鍵上面印的符號、
  ② `Ctrl ＋ 鍵` 打中文標點。判斷方法：**先看鍵盤上有沒有印**。
  而且開頭直接回扣前面那句「Ctrl 一定要配別的鍵一起按」——**這就是它的第一個用法**，前後就連貫了。
- **虛擬鍵盤現在出現三次**（打字區一張、編輯鍵區一張、原本第 2 節那張）。
  沒有複製 HTML —— typing.js 會自動把第一張複製到 `<div class="kbdbox" data-copy></div>`，
  三張完全一樣（各 109 顆鍵）且**各自獨立**（在一張切分區不會影響另外兩張）。
- **實驗一改成完整的數字鍵盤**：17 顆鍵（含 / * - + Enter），
  其中 11 顆有雙重身分會跟著 Num Lock 變（7→Home、1→End、0→Ins、.→Del⋯⋯）。
- **六個大寫字母改成 H S P S T W**（老師指定；原本想拼 TAIWAN 但 A 重複不適合）。
- 版本號：`typing.js?v=5`

**要注意的事（這次新增）**
- ⚠️ 課文要再放同一張鍵盤，**不要複製 HTML**，放 `<div class="kbdbox" data-copy></div>` 就好。
  `data-copy="edit"` 還可以預選某一區。複製是在繫結事件「之前」跑的，所以複製出來的也會動。
- ⚠️ **預覽視窗被節流時，CSS transition 不會跑完**，`getComputedStyle` 會一直讀到起始值。
  這次因此誤判「分區變暗壞掉」。要驗證有 transition 的樣式，
  先塞一段 `transition:none !important` 再量，才不會被假象騙。

---

## 2026-08-17（第五次）· 主力電腦（D:\Claude）

**做了什麼**（第 1 課再修三項）
- **補上漏掉的功能**：Enter 的「**送出**」（搜尋框、聊天室、表格按了就送出去）、
  Shift 的「**切換中文／英文**」（學生「突然打不出注音」最常見的原因）。
  互動鍵盤上那兩顆的說明也一起補。
- **打字區不再分三個小標**（會打出東西／會刪掉東西／自己按沒有用的三顆），
  統一成一個 **「打字區的功能鍵」**，六張卡片並列：
  空白鍵、Enter、Backspace、Tab、Shift、Ctrl 和 Alt。
- **符號測試的「，」「。」改成只認中文全形**：
  打英文的 `,` `.` 會判錯，並提示「你現在是英文輸入法，按一下 Shift 換成中文」。
  驚嘆號和問號**維持兩種都算對**（它們是在練 Shift，不是練標點）。
  後面加一段揭曉，講中文標點和英文標點長得不一樣、寫作文要用中文的。
- 版本號：`typing.css?v=4`、`typing.js?v=4`

**要注意的事（這次新增）**
- ⚠️ `symtest` 新增 **`data-strict`**：加了就不做全形半形換算。
  中文標點那兩格一定要加，不然學生打英文標點也會被判對。
- ⚠️ **`.lampbd` 裡的 `.krow` 不能用大鍵盤那套 `flex:var(--w) 0 0`**。
  大鍵盤的主鍵盤區有固定寬度的格線欄可以分，迷你鍵盤是 `width:max-content`，
  `flex-basis:0` 會讓整排塌掉（實測鍵寬從 46px 變成 17px）。
  已改成 `flex:0 0 calc(var(--u) * var(--w,1))` 直接算寬度。

---

## 2026-08-17（第四次）· 主力電腦（D:\Claude）

**做了什麼**（老師看過第 1 課之後的七項修正）
- **修好模擬鍵盤破版**：數字鍵盤會凸出深色底板、還跑出橫向捲軸。
  原因是 `.kbd` 寫死 `min-width:23u`，但實際內容約 **25.1u**（分區內距和格線
  間距都會加寬）。改成 `width:max-content` 讓底板自己包住內容，
  並把 `--u` 調成 `clamp(20px,3.6vw,40px)`。現在填滿度 99%、零捲軸、零裁字。
- **修好 `<b>` 顯示成文字**：`lesson.js` 的預測題回饋用的是 `textContent`，
  我在 30 個 `data-say` 裡寫了 `<b>`，全部變成字面標籤。
  改成 `innerHTML`（排序題的 `data-msg-*` 也一起改）。**17 課 Scratch 也受惠。**
- **打字區大幅擴寫**：原本只講 Shift 和空白鍵，現在七顆一顆一顆講 ——
  空白鍵、Enter、Backspace、Tab，加上「自己按沒有用的三顆」Shift／Ctrl／Alt
  （比喻成「鍵盤的語氣」），並強調組合鍵要「按住」。
- **符號測試加入「，」「。」**：這兩個**不用按 Shift**，跟驚嘆號問號剛好對比，
  後面附一段「看鍵盤就知道要不要按 Shift」的揭曉。
- **功能鍵照片移到「試試看」前面**（先看到鍵在哪，再動手試）。
- **編輯鍵區精簡**：坦白說「這 6 顆現在很少用」，只留 Delete／Home／End 重點，
  但**游標實驗室完整保留**。
- **三顆燈改版**：老師新拍的 `鍵盤燈對應.webp`（標出 Caps Lock 和 Num Lock
  各控制哪一區）放在最前面。兩個實驗改成**迷你鍵盤**（跟大鍵盤同一套視覺），
  只放會用到的鍵、其餘用 `.kk--dim` 淡化，**NumLk／Caps Lock 鍵本身就是開關**，
  按下去右上角的燈會跟著亮暗。
- **新增六個大寫字母測試**：拼出 **TAIWAN**，Caps Lock 或 Shift 兩種方法都可以，
  後面揭曉兩者差別（鎖住 vs 按著才有，以及 Caps Lock 對符號無效）。
- 版本號再 +1：`lesson.css/js?v=4`、`typing.css/js?v=3`、`home.css?v=4`

**要注意的事（這次新增）**
- ⚠️ `.kbd` **不要再寫死 `min-width`**。整個鍵盤約 25.1u 寬，
  寫死的數字跟實際內容對不起來，數字鍵盤就會凸出底板。
  要調大小改 `--u` 的 clamp 就好。
- ⚠️ `data-say` / `data-miss` / `data-msg-*` 現在**都吃 HTML**。
  內容是我們自己寫的、沒有使用者輸入，所以用 `innerHTML` 是安全的。
- ⚠️ `symtest` 的 `data-want` 可以用 `|` 列多個答案。
  **「。」一定要寫成 `data-want="。|.|｡"`** —— 它不在 U+FF01–FF5E 裡，
  全形換半形的公式換算不到，只靠公式會把學生打對的判成錯。

---

## 2026-08-17（第三次）· 主力電腦（D:\Claude）

**做了什麼**
- 老師拍好素材並用壓縮工具壓過（7～63 KB，都遠低於目標），**8 張全部接進打字第 1 課**
  - 建立 `網站/media/`，圖片都放這裡
  - 開場故事加了三張：**英文打字機 → 外國漫畫的誤解 → 真正的中文打字機**
    這個對比很有力：漫畫以為「幾萬個字就要幾萬個鍵」，
    真機器其實**一個鍵都沒有**，是用「找」的。中間插一題預測讓學生先猜。
  - 第 6 節放數字鍵盤特寫，用照片裡**印在鍵上的第二個名字**（7 Home、8 ↑⋯⋯）
    當證據，讓學生自己發現「這些鍵有兩種身分」，再接到第 7 節的 Num Lock
  - **三顆燈刻意不放照片**（老師指定）——燈太小，照片看不出亮暗差別，
    改成「抬頭看真的那台」＋ 講義裡的模擬器
- 第 3 節（打字區）新增 **符號輸入測試 `symtest`**：
  「請打出 ？」學生真的在框裡打一次，要自己找鍵、想到按 Shift
- `lesson.css` 新增 **`.shot--done` ＋ `figcaption`**：素材補上去之後，
  佔位框的「要拍什麼／怎麼拍」那些對學生是雜訊，換成正常圖說。
  **17 課 Scratch 之後補圖時也用這個**
- 版本號全部 +1：`lesson.css/js?v=3`、`typing.css/js?v=2`、`home.css?v=3`（19 頁）

**要注意的事（這次新增）**
- `symtest` 有兩個一定要處理的細節，改的時候不要拿掉：
  1. **全形／半形**——中文輸入法打出來是 `？`，英文是 `?`，
     程式會把全形（U+FF01–FF5E）減 0xFEE0 換成半形再比對，**兩種都算對**
  2. **組字中不判定**——打注音時輸入框會先出現ㄅㄆㄇ，
     要等 `compositionend` 才比對，不然學生打到一半一直看到 ✗

---

## 2026-08-17（第二次）· 主力電腦（D:\Claude）

**做了什麼**
- 開了新系列「**中文打字**」，寫好 `docs/中文打字-課程地圖.md`
- 定案為 **4 節**（老師原本想 3 篇，第一篇估時 25 分會爆掉，拆成兩節）
  1. 鍵盤大導覽（五區 ＋ 三顆燈）2. 注音怎麼變成中文字
  3. 手不要離開鍵盤（改錯字＋快捷鍵＋截圖）4. 輸入法大觀園
- **第 1 課做完了**：`網站/typing/01-鍵盤大導覽.html`
- 新增 `網站/assets/typing.css` ＋ `typing.js`（打字系列專用，Scratch 課不載入）
  做了三個新互動：**會動的鍵盤全圖**（點分區會亮）、**三顆燈實驗**
  （關掉 Num Lock 數字真的打不出來）、**游標實驗室**（Backspace／Delete／Insert 陷阱）
- 首頁加上「中文打字」課程區塊，資訊素養那區的「打字練習」拿掉避免重複
- `網站/README.md` 補上新元件用法；**修正版本號指令**（原本只掃 `lessons\`，
  漏掉新的 `typing\` 和 `index.html`）
- `素材清單.md` 加上打字第 1 課的 6 張素材（多半是實體鍵盤照片）

- 做了 **`網站/tools/圖片壓縮.html`**（首頁「給老師」區塊有連結）
  - 拖放圖片 → 自動縮到 1600px → 壓縮 → 顯示省了幾 %，用顏色標有沒有達標
  - **單一檔案、零外部相依**，可以丟到隨身碟在任何電腦離線使用
  - 實測：2560×1440 的 3.7 MB 截圖 → WebP 100 KB（省 97%）、JPEG 172 KB（省 95%）
  - **遇到 GIF 會擋下來不處理**（見下方注意事項）
  - ⚠️ **這是一個通用工具，刻意不寫任何講義相關的東西**（怎麼接進課頁、
    素材檔名規則⋯⋯那些寫在 `素材清單.md`）。之後要加功能請維持這個界線。
- 做了 **`網站/tools/圖片裁剪.html`**（同樣是通用工具、單一檔案、零外部相依）
  - 拖曳裁剪框、八個把手改大小、比例鎖定（1:1／16:9／4:3／3:4／9:16／原圖比例）
  - 方向鍵可以一格一格微調（配 Shift 一次 10 格），轉向 90°（手機照片常常是躺著的）
  - 實測：座標換算精準（移動 200 原圖像素＝ 67.7 顯示像素）、
    拖出界會夾在圖片邊緣、鎖比例時亂拉比例都維持在 1.778
  - 兩個工具互相連結：**先裁再壓**
- `素材清單.md` 補上建議大小的表格、壓縮工具用法、`loading="lazy"` 的提醒

**下次要做什麼**
- 做打字第 2 課「注音怎麼變成中文字」——核心是**注音鍵位的直行規律**
  （ㄅㄆㄇㄈ＝1QAZ 一直行），地圖裡已經寫好完整的對照表
- 第 2 課要新做 `bpmkeys`（滑過會亮整條直行）和 `typerace`（打字練習）兩個元件

**要注意的事**
- ⚠️ **GIF 不能用瀏覽器壓縮**。把圖畫到 canvas 再輸出，動畫只會剩第一格，
  而我們的素材有 39 個是 GIF。所以壓縮工具遇到 GIF 會**擋下來並說明**，
  不會產生下載連結（避免老師拿到被毀掉的檔案）。
  GIF 要在 **ScreenToGif 錄的時候**就調小，事後救不回來。
- 圖片建議大小：截圖 200–400 KB、照片 150–300 KB、GIF 0.8–2 MB，
  **整頁加起來不超過 3 MB**（限制是學校網路速度，不是容量）
- 裁剪工具的 `.stage` 上有 `overflow:hidden`，**不要拿掉** ——
  裁剪框是用一個 9999px 的 `box-shadow` 把框外壓暗的，
  沒有那個裁切的話陰影會蓋滿整個頁面。把手也刻意畫在框「裡面」，
  不然框拖到圖片邊緣時會被切掉一半。
- ⚠️ **`typerace` 有個大坑**：學生打注音時輸入框會有「組字中」狀態，
  直接聽 `input` 事件會把組字中的注音判成打錯。**一定要用
  `compositionstart` / `compositionend`**，只在組字結束後才比對。
- ⚠️ 講義裡有兩處**史實與字碼要老師覆核再上線**：第 1 課故事的年份
  （林語堂 1947、朱邦復 1976）、第 4 課的倉頡字碼。**無蝦米的字碼沒有可靠來源，
  講義不會寫死按鍵**，只講「用字形像的英文字母」這個原則。
- 第 3 課的截圖鍵 `Win+Shift+S` 是作業系統層級的，**網頁收不到那個按鍵**，
  所以不能做成按鍵偵測練習，要改用 GIF 示範
- 新增了 `.claude/launch.json`（本機預覽用，跟講義內容無關）

---

## 2026-08-17 · 主力電腦（D:\Claude）

**做了什麼**
- 完成全部 17 課講義，互動元件全部實測通過
- 完成課程總覽首頁 `網站/index.html`，含三個預留課程區塊
  （Scratch 進階 / Arduino / 資訊素養）
- 每一課加上「← 課程總覽」返回連結
- 建立 GitHub repo `ggelgood/course`，設定 GitHub Actions 自動發布
- 網站上線：https://ggelgood.github.io/course/
- 產出 `素材清單.md`（50 個待拍素材，含檔名與拍攝說明）
- 加上 `START-HERE.md` 接手指南與這份工作日誌

**下次要做什麼**（老師還沒決定要先做哪一個）
- 拍素材，然後接進講義（50 個，優先拍標 ★ 的 15 個）
- 或製作範例 `.sb3` 檔案放進 `網站/files/`
- 或開始新的課程單元（Scratch 進階／Arduino／資訊素養）

**要注意的事**
- 素材資料夾 `網站/media/` 和 `網站/files/` 目前是空的，
  git 不追蹤空資料夾，所以在 GitHub 上看不到它們，這是正常的
