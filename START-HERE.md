# START-HERE — 給 Claude 的接手指南

> **老師使用方式**：在新電腦上打開 Claude Code，工作目錄指向這個資料夾，然後說：
> 「**讀 START-HERE.md，然後我們繼續**」

---

## 這是什麼專案

一個**國小中年級（三、四年級）Scratch 教學講義網站**。老師是國小電腦課老師，
用 Claude 協助製作講義，學生在電腦教室自學使用，老師開場投影、中間巡堂。

**17 課已全部完成並上線**，網址：`https://ggelgood.github.io/course/`
（GitHub Pages 自動發布，只要 push 到 main，網站約 1–2 分鐘後自動更新。）

---

## ⚠️ Claude：接手時第一件事（在讀任何東西之前）

老師會在**兩台電腦之間來回切換**，最怕的就是進度不同步。
**請主動執行下面的檢查，不要等老師問**：

```bash
git status -sb | head -1
git log --oneline -3
```

然後依結果主動告訴老師：

| 看到什麼 | 意思 | 你要說什麼 |
|---|---|---|
| `## main...origin/main` | 完全同步 | 「同步狀態正常，可以開始」 |
| `[behind N]` | **另一台電腦有新東西還沒抓下來** | 「⚠️ 這台電腦落後 N 筆，請先在 GitHub Desktop 按 **Pull origin**，不然會改到舊版」 |
| `[ahead N]` | 這台有東西還沒推上去 | 「這台有 N 筆還沒 push，等一下結束前記得推」 |
| `[ahead N, behind M]` | **兩邊都有各自的修改，分岔了** | 「⚠️ 兩台電腦都改過東西，先不要動，我幫你處理」（見下方「出事了怎麼救」）|
| 有列出檔名 | 有沒存檔的變更 | 「有 N 個檔案改過還沒存檔，要先處理嗎？」 |

**如果是 `behind` 或分岔，先解決再開始工作**，不要直接動手改檔案。

接著看 **`工作日誌.md`** 最上面幾筆，就知道上次做到哪、下次原本要做什麼。

---

## 接手時請依序讀這幾份

1. **`工作日誌.md`** — 先看最上面兩三筆，掌握進度與上次卡在哪
2. **`docs/課程地圖.md`** — 最重要。17 課的完整規劃、教學設計原則、
   每一課教什麼／刻意不教什麼、以及背後的理由。**做任何內容決定前先讀這份。**
3. **`網站/README.md`** — 技術面。所有互動元件的用法範例、資料夾結構、
   維護規則（特別是版本號機制）、做完一課的自我檢查清單。
4. **`素材清單.md`** — 50 個待補的截圖與 GIF，含檔名與拍攝說明。

讀完就能完整接手，不需要老師重新解釋設計脈絡。

---

## ⚠️ Claude：工作結束前要做的事

1. ⛔ **不要主動寫工作日誌**。老師說「**記錄一下**」或「**寫日誌**」才寫，
   而且**寫短的**：日期、做了什麼（3–5 行）、下次要做什麼、踩到的坑。
   不要把每個修改細節都寫進去 —— 那些看 git log 就好。
2. **提醒老師 commit + push**，並說明一句為什麼：
   「這樣明天在另一台電腦按 Pull 就會拿到最新版」
3. push 需要 GitHub 帳號驗證，**Claude 這邊執行 `git push` 會卡在登入提示**，
   所以請老師在 **GitHub Desktop 按 `Push origin`**，不要自己嘗試 push

---

## ⚠️ Claude：省 token 的工作方式

製作教材很容易把 token 燒光。下面每一條都是**實際浪費過**才定下來的，請遵守。

### 1. 版本號收工前才 bump 一次

開發中改 `assets/` **不要每次都 bump**（本機重新整理就看得到最新的）。
**等一個段落全部做完、要交給老師 commit 之前，才一次把版本號 +1。**

> 踩過：一個 session 內 bump 了 5 次（v1→v5），每次都改寫全站 19 個檔案。

### 2. 改檔案用 Edit 工具，不要用 PowerShell / Bash

用腳本去改「Claude 已經讀過」的檔案，系統會把**整個檔案回貼進對話**，一次 200＋ 行。
**只有全站批次取代**（例如版本號要掃 19 個檔）才值得用腳本，而且放到最後再做。

### 3. 工具壞掉就放棄，不要重試

截圖、預覽這類工具**連續失敗 2 次就永久停用**，改用資料驗證，
並在回覆裡誠實告訴老師「這次沒能親眼看畫面，版面麻煩你確認」。

> 踩過：截圖壞掉後重試 8 次以上，每次逾時 30 秒。

### 4. 驗證要量「行為」，不要量「動畫跑完後的樣子」

查 `dataset` 有沒有變、class 有沒有加、文字對不對 —— 這些是**同步的、不會騙人**。
**不要去量 transition 之後的 computed style**（opacity、filter⋯⋯）：
預覽視窗被節流時動畫不會跑完，會一直讀到起始值。

> 踩過：因此誤判三次「功能壞掉」，結果三次都是好的。
> 真的必須量，先塞一段 `transition:none !important` 再量。

### 5. 一批改完才驗一次

不要每改一處就掃一次全頁。**改完一整批再做一次完整驗證。**

### 6. 回覆寫短一點

條列重點就好，不要每次都做大表格和多層標題。
只講三件事：**改了什麼、發現什麼問題、有什麼要老師決定**。

### 7. 新課先給大綱，確認後再寫內容

要做新的一課，**先給老師一頁大綱**（每節標題 ＋ 每節放哪些元件 ＋ 估時），
等老師點頭再寫完整內容。

> 踩過：打字第 1 課一次寫完 645 行，之後前後改了 17 項，
> 其中「打字區篇幅太少」「編輯區太多」「三顆燈不要用照片」全都是**結構問題** ——
> 大綱階段就抓得到，不必重寫整段。

---

## 目前進度

| 項目 | 狀態 |
|---|---|
| 17 課講義 | ✅ 全部完成，互動元件實測通過 |
| 課程總覽首頁 | ✅ 完成，含三個預留課程區塊 |
| GitHub Pages 部署 | ✅ 已上線，push 後自動更新 |
| **截圖／GIF 素材** | ⬜ **50 個全部待補**（詳見 `素材清單.md`） |
| 範例 `.sb3` 檔案 | ⬜ 待製作，放進 `網站/files/` |
| Scratch 進階 / Arduino / 資訊素養 | ⬜ 首頁已預留區塊，內容未開始 |

**最新進度以 `工作日誌.md` 為準**（這張表可能沒跟上）。

---

## 資料夾結構

```
課程講義/
├── START-HERE.md          ← 這份檔案
├── 素材清單.md             ← 50 個待拍素材
├── docs/課程地圖.md        ← 教學設計文件（最重要）
├── 網站/                   ← 發布出去的網站本體
│   ├── index.html          ← 課程總覽首頁
│   ├── assets/             ← lesson.css / lesson.js / home.css（共用）
│   ├── lessons/            ← 01～17 課
│   ├── media/              ← 截圖與 GIF（目前空的）
│   ├── files/              ← 學生下載的 .sb3（目前空的）
│   └── README.md           ← 技術維護說明
├── 原型/                   ← 早期版本，已被「網站/」取代，可忽略
└── handoff/                ← 最早的交接檔案，已過時，可忽略
```

---

## 工作慣例（這些都是踩過坑才定下來的，請遵守）

### 教學設計

- **每節最多一個「重概念」**（事件、迴圈、廣播、條件），或 2–3 個「輕概念」
- **時間用「變化」填，不用「概念」填** — 同一組積木的不同玩法，而不是加新積木
- **作品節（第 6、9、13、15 課）不放測驗** — 沒有排序題、預測題、Python 面板，
  改用三關分級（一定要做到／做完再加／還有時間就挑戰）吸收快慢差距
- **故障排除絕對不要放進 `<details>`** — 學生卡住時的反應是舉手，
  不是去點開一個他不知道存在的區塊

### 講義的文字（老師逐字改過，請照規則寫）

- **不用破折號（——）補充說明**。一句話講完一件事，
  該斷句就用句號或逗號斷開，不要「主句 —— 補充」這種寫法。
  這是講義給學生看的，不是講給自己聽的自言自語。
- **不用口語詞**，例如「幹嘛的」要寫成「功用」。
  講義是正式文件，用詞要跟聊天分開。
- 寫完一段，要站在**學生的角度**重讀一次：這句話是不是在幫學生理解，
  還是只是我在對自己解釋？多餘的補充句該刪就刪。

### 出題原則（最容易犯錯的地方）

**排序題的正確答案，絕對不能出現在題目上方的積木堆裡。**
老師抓過這個問題：上面秀出完整順序、下面叫學生排回同樣順序，
學生往上瞄一眼就照抄，這題完全沒有價值。

正確做法：
1. 用**文字描述情境**，且順序要跟課文教的**不一樣**，逼學生讀題
2. 兩塊內容不同但外型相同的積木（例如兩個「說出」）效果最好

### 預測題的回饋

每個錯誤選項的 `data-say` 要**寫出那個選擇背後的誤解**，不要只寫「再想想看」。
學生答錯時要有收穫，老師巡堂時也才能從他選了什麼看出他卡在哪。

### Python 對照面板

- **只在積木第一次出現、而且 Python 真的有對應寫法時才附**
- **沒有對應的一律不附**（綠旗、廣播、當按鍵被按下、碰到邊緣反彈、面朝、尺寸）
  — 不要寫「Python 沒有這個東西」那種只有註解的空面板
- 用真的能跑的 `turtle` 程式，不要寫假的虛擬碼

### 技術

- **改完 `網站/assets/` 裡的檔案，要把所有 HTML 的 `?v=` 版本號 +1**
  不然學生的瀏覽器會繼續用快取的舊版。這個坑踩過兩次。
  指令寫在 `網站/README.md`。
  ⚠️ **但開發過程中不要每改一次就 bump** —— 收工前一次做完就好（見下方省 token 規則第 1 條）。
- **不要複製樣式到個別課頁** — 所有樣式在 `assets/lesson.css`，改一次全部生效
- 課別專用的小互動（滑桿、轉盤）寫在該課 HTML 的 `<script>` 裡，
  不要塞進 `lesson.js`

---

## 做完一課之後的自我檢查

老師特別要求過的四項，每次做完都要跑：

1. **排序題的答案有沒有外洩**（正確順序出現在題目上方了嗎）
2. **有沒有偷跑到後面的課**（教了地圖上屬於後面課次的東西）
3. **跨課指涉對不對**（「下一課」「上禮拜」「第 N 課」有沒有寫錯）
4. **每個互動是不是真的需要思考**（能不能照抄上面的答案就過關）

---

## 溝通偏好

- **一律用繁體中文**，專有名詞（Scratch、CSS、GitHub 等）保留原文
- 老師會直接說「方向不對」，這時**不要硬推自己的方案**，先問清楚再重來
- 提出建議時給**明確推薦**，不要只列選項讓老師自己選

---

## 出事了怎麼救

### 情況一：這台電腦落後（`behind N`）

**最常見，也最好處理。** 請老師在 GitHub Desktop 按 **`Pull origin`** 就好。
前提是這台電腦沒有自己改過東西（`git status` 是乾淨的）。

### 情況二：兩台電腦都改過，分岔了（`ahead N, behind M`）

發生原因：在 A 電腦改完**忘記 push** 就走了，隔天在 B 電腦 pull（拿到舊版）
又改了東西並 push，於是兩邊各自有不同的紀錄。

**處理原則：先確認兩邊改的是不是同一個檔案。**

```bash
git fetch origin
git log --oneline main..origin/main      # 遠端有、本機沒有的
git log --oneline origin/main..main      # 本機有、遠端沒有的
git diff --name-only main origin/main    # 兩邊差異的檔案清單
```

- **如果改的是不同檔案** → 直接 `git pull --no-rebase`，git 會自動合併，通常沒事
- **如果改到同一個檔案** → 會產生衝突。**先把兩個版本都列給老師看**，
  問他要保留哪一邊，不要自己決定。可以用
  `git show origin/main:檔案路徑` 看遠端版本、直接讀檔案看本機版本

### 情況三：有一堆沒存檔的變更，但不確定是什麼

```bash
git status --short          # 看有哪些
git diff                    # 看改了什麼內容
```

**不要隨便用 `git checkout` 或 `git reset --hard` 丟掉**——那會永久刪除老師的心血。
先看清楚內容，問過老師再處理。

### 情況四：不小心把東西弄壞了，想退回上一個好的版本

```bash
git log --oneline -10       # 找到那筆好的紀錄
```

然後問老師要退到哪一筆，再處理。**git 的好處就是每一筆 commit 都留著**，
只要曾經 commit 過就救得回來。

---

## 給老師的簡單版規則

如果只記得一件事，記這個：

> **開工前先在 GitHub Desktop 按 `Fetch origin`，看到 `Pull origin` 就點下去。
> 收工前一定 `Commit` + `Push`。**

只要每次結束前都推上去，換哪台電腦都不會有問題。
**分岔幾乎都是「忘記 push 就換電腦」造成的。**

如果不確定現在狀態如何，直接問 Claude：「幫我看一下同步狀態」。

---

## 怎麼驗證改動

網站是純靜態的，本機測試最簡單的方式：

```bash
python -m http.server 8765 --directory 網站
```

然後開 `http://localhost:8765/`。

Claude Code 內建的預覽窗格會把本機檔案轉成 `data:` URL，
**相對路徑（`../assets/lesson.css`）在那個模式下會失效**，
所以驗證一定要用上面的 HTTP 伺服器，不要用 file:// 或預覽快照。
